import type { NextPage } from 'next'
import Head from 'next/head'
import styles from '../styles/Home.module.css'


const Graphics = (props:string[]) =>{
    return(
        <div>
            
        </div>
    );
}

export default Graphics;